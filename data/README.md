# Data

The committed notebook uses a **synthetic engagement dataset** generated in-notebook. If migrating to a real public engagement dataset, place the file here and update the loader. Raw data is git-ignored.
